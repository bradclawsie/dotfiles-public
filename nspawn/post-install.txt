# set root passwd from local shell
sudo systemd-nspawn -D /var/lib/machines/{$machine} /usr/bin/passwd

# do sudo systemd-nspawn -b -D /var/lib/machines/{$machine} to log in as root and do...
/usr/sbin/useradd -m -s /usr/bin/fish brad
passwd brad
apt install locale sudo tmux git fish
/usr/sbin/usermod -a -G sudo brad
/usr/sbin/dpkg-reconfigure locales

# do this as root AND as brad
systemctl --user enable ssh-agent.service
systemctl --user start ssh-agent.service

loginctl enable-linger brad

# make a tarball of stuff to cp over
tar -rvf d.tar .gitconfig .tmux.conf .ssh .gnupg .config/fish .config/nvim bin/pull-git.pl bin/push-git.pl repos/bradclawsie/dotfiles-public/.git/config

